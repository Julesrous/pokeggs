<div class="navbar navbar-expand-lg navbar-light bg-light">
  <a href = "../"> <p> Pokegg </p> </a>
  <% if user_signed_in?%>
  <%= link_to "Search", pokeggs_path %>
  <%= link_to "Create", new_pokegg_path%>
    <div class="nav-item dropdown">
    <%= cl_image_tag current_user.photo.key, height: 50, width: 50, crop: :fill, class: "avatar-current-user dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
       <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "dropdown-item" )%>
       <%= link_to "my profile", user_path(current_user.id), class: "dropdown-item" %>
       </div>
      </div>
    <% else %>
    <div>
      <%= link_to "Sign in", new_user_session_path, class: "mr-3" %>
      <%= link_to "Sign up", new_user_registration_path %>
    </div>
  <% end %>

</div>

